spring:
  datasource:
    url: ${POSTGRES_CLUSTER_URL}
    username: ${POSTGRES_CLUSTER_USERNAME}
    password: ${POSTGRES_CLUSTER_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger